fastapi
uvicorn[standard]
sqlmodel
pydantic
pydantic-settings
python-dotenv
typer
requests
beautifulsoup4
pypdf
PyMuPDF
keyring
# Knowledge Base document extraction + multipart uploads.
python-docx
openpyxl
python-multipart
# Optional cloud drafting provider for the Knowledge Base AI Response Workspace.
# Imported lazily, so the app and full test suite run without it installed; only
# needed when a user configures Claude API drafting. Local Ollama remains the
# default provider and requires no cloud key.
anthropic
# playwright powers assisted-login authenticated sources (e.g. PlanetBids).
# It is imported lazily, so the app and tests run without it. After install,
# run the one-time browser download: `playwright install chromium`.
playwright
