# Desktop Icons

Place real application icon files here when available:

- `icon.png` for Linux and development fallback.
- `icon.icns` for macOS packaging.
- `icon.ico` for Windows packaging.

The launcher uses these files when present. No generated logo is included.
