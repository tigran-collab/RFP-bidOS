The app now has opportunity database models, frontend API connection, rules-based scoring, public scraper foundation, document downloader, pypdf PDF parsing fallback, and local AI opportunity evaluation through Ollama.

Implement structured RFP requirements extraction and compliance matrix generation.

Goal:
Use extracted document text, opportunity metadata, and local Ollama AI to identify key solicitation requirements and create human-reviewable compliance matrix records.

Do not use OpenAI API.
Do not use cloud AI.
Do not add OCR.
Do not add Playwright.
Do not add login scraping.
Do not add recursive crawling.
Do not add automated submission.
Do not generate full proposal drafts yet.
Do not run uvicorn --reload as an automated check.

Important:
This phase is about extracting requirements and creating a compliance matrix, not writing the proposal.

Backend requirements:

1. Database models

Add or update models as needed.

Create Requirement model/table if not already complete:

* id
* opportunity_id
* document_id nullable
* requirement_type
* title
* requirement_text
* source_page nullable
* source_section nullable
* mandatory boolean default true
* due_date nullable
* status
* assigned_response_section nullable
* notes nullable
* created_at
* updated_at

Suggested requirement_type values:

* Submission Requirement
* Deadline
* License
* Insurance
* Bond
* Form
* Attachment
* Evaluation Criteria
* Scope Requirement
* Staffing Requirement
* Training Requirement
* Reporting Requirement
* Pricing Requirement
* Contract Term
* Pre-Bid Requirement
* Q&A Requirement
* Other

Suggested status values:

* Needs Review
* Compliant
* Non-Compliant
* Not Applicable
* Missing Information

2. Local AI extractor service

Create:

backend/app/services/requirement_extractor.py

Implement:

* build_requirement_extraction_prompt(opportunity, extracted_text_snippets) -> str
* load_extracted_text_for_requirements(opportunity_id, session, max_chars=25000) -> list[dict]
* extract_requirements_with_local_ai(opportunity_id, session) -> dict
* save_extracted_requirements(opportunity_id, extraction_result, session) -> list[Requirement]
* parse_requirements_json_response(response_text: str) -> dict

Use Ollama local API only:

POST http://127.0.0.1:11434/api/generate

Payload:
{
"model": model_name,
"prompt": prompt,
"stream": false,
"format": "json",
"options": {
"temperature": 0.1
}
}

Use existing OLLAMA_MODEL and OLLAMA_BASE_URL config if already implemented.

3. AI prompt behavior

The prompt must instruct the model:

You are extracting requirements from government RFP documents for a security services contractor.

Extract only requirements supported by the provided text.
Do not invent missing requirements.
If page numbers or sections are visible in the text, include them.
If page numbers are not clear, leave source_page null.
Distinguish mandatory requirements from optional/descriptive language.
Flag missing or unclear information.

Return valid JSON only.

Required JSON structure:

{
"summary": "short summary of extracted requirements",
"requirements": [
{
"requirement_type": "Submission Requirement | Deadline | License | Insurance | Bond | Form | Attachment | Evaluation Criteria | Scope Requirement | Staffing Requirement | Training Requirement | Reporting Requirement | Pricing Requirement | Contract Term | Pre-Bid Requirement | Q&A Requirement | Other",
"title": "short requirement title",
"requirement_text": "exact or closely paraphrased requirement",
"source_page": null,
"source_section": null,
"mandatory": true,
"due_date": null,
"status": "Needs Review",
"assigned_response_section": null,
"notes": null
}
],
"missing_information": [],
"risk_flags": []
}

4. Extraction targets

The extractor should look especially for:

* proposal due date
* submission method
* portal requirements
* required forms
* required attachments
* license requirements
* insurance requirements
* bonding requirements
* pre-bid meeting details
* mandatory pre-bid status
* Q&A deadline
* addenda acknowledgment
* pricing form requirements
* staffing requirements
* training requirements
* background check requirements
* reporting requirements
* technology/reporting requirements
* contract term
* renewal options
* evaluation criteria
* minimum qualifications
* references
* financial stability requirements
* exceptions/deviations requirements
* disqualifying conditions

5. API routes

Add routes:

POST /opportunities/{id}/extract-requirements

This should:

* load opportunity
* load extracted text
* call local Ollama
* save requirements
* return extraction summary and saved requirements

GET /opportunities/{id}/requirements

This should return saved requirements.

POST /opportunities/{id}/requirements/refresh

This should delete existing AI-extracted requirements for the opportunity and run extraction again.

6. CLI commands

Add:

python -m app.cli extract-requirements <opportunity_id>
python -m app.cli extract-all-requirements

CLI output should include:

* opportunity title
* number of requirements extracted
* number of missing information items
* risk flags

7. Frontend

On OpportunityDetail page, add a Requirements / Compliance section.

Include:

* button: Extract Requirements
* requirements table with:

  * Type
  * Title
  * Mandatory
  * Status
  * Source Page
  * Assigned Section

Show requirement detail text in expandable form or readable block.

Add simple status badges:

* Needs Review
* Compliant
* Non-Compliant
* Not Applicable
* Missing Information

Do not make it overly complex yet.

8. Error handling

If Ollama is not running, return clear error:

"Local AI model is not available. Start Ollama and make sure the model is installed."

If no extracted text exists, return:

"No parsed document text is available for this opportunity. Download and parse documents before extracting requirements."

If model returns invalid JSON:

* return clean error
* include raw response internally if existing pattern supports it
* do not crash backend

9. README update

Add usage instructions:

* Parse documents first:
  python -m app.cli parse-opportunity-documents <id>

* Extract requirements:
  python -m app.cli extract-requirements <id>

* Extract all:
  python -m app.cli extract-all-requirements

Explain that extraction is local-AI-assisted and requires human review.

10. Verification commands

Run:

python -m pip install -r requirements.txt
python -m app.cli init-db
python -m app.cli seed-demo
python -m app.cli parse-all-documents
python -m app.cli extract-all-requirements
python -m app.cli ai-evaluate-all-opportunities
npm.cmd run build

Do not run uvicorn --reload as a long-running automated verification command.
